console.log('hello there')
// need to get all my box's and give them variables
let redBox = document.getElementById('topleft')
let greenBox = document.getElementById('topright')
let blueBox = document.getElementById('bottomleft')
let yellowBox = document.getElementById('bottomright')
// need to have an empty array to fill with random numbers
let computerArr = [];
// need to have an array to match with computer to make sure i did the right combo
let myArr = [];